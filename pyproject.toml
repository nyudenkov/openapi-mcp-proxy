[project]
name = "openapi-mcp-proxy"
version = "0.1.0"
description = "MCP Server for OpenAPI Schema Exploration"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "aiofiles>=24.1.0",
    "httpx>=0.28.1",
    "mcp>=1.12.3",
    "pydantic>=2.11.7",
]
